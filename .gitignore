# Python
__pycache__/
*.pyc
.venv/
venv/
backend/__pycache__/
backend/.pytest_cache/
backend/data/store.json
backend/data/store.db
backend/data/store.db-shm
backend/data/store.db-wal
backend/data/*.db*
backend/data/classifier.joblib

# Node
node_modules/
frontend/build/
frontend/dist/
# frontend/dist was previously committed by mistake; untracked 2026-08-06
npm-debug.log*
yarn-error.log*
package-lock.json
frontend/package-lock.json

# Local frontend assets generated or copied during development
frontend/public/app-icon-*.png
frontend/public/apple-touch-icon.png
frontend/public/favicon.ico
frontend/public/logo.png
# frontend/public/manifest.json  # Uncomment if you want to keep manifest local

# Local images and scratch files
ChatGPT Image*.png
a505b0cb-*-cover.png
logo*.png

# Local-machine / agent session artefacts (not for the repo)
.claude/
scratch/

# AI / project docs (not for the repo)
CLAUDE.md
TASK.md

# Android / Capacitor (mobile APK build)
frontend/android/
frontend/capacitor.config.json

# Env — single project-root .env (keep .env.example, never commit real keys)
/.env
.env.local
atlas-credentials.env
# (legacy per-folder env files, in case any linger)
backend/.env
frontend/.env

# Local backend data files
backend/data/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs and temporary files
debug-*.log
*.log
*.out
*.err
backend/uvicorn.out

# Test artifacts
.pytest_cache/
pytest-basetemp*/
.tmp/

# Uploaded Excel files
Expenditure*.xlsx
